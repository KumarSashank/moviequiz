spring.application.name=backend
spring.cloud.gcp.credentials.location=inline    # if using Spring Cloud GCP
spring.cloud.gcp.credentials.encoded-key=${GOOGLE_APPLICATION_CREDENTIALS_JSON}
spring.cloud.gcp.firestore.project-id=${FIRESTORE_PROJECT_ID}
